# 배포하기 — 인터넷에 블로그 올리기

처음 한 번만 설정하면, 이후에는 글을 고칠 때마다 **자동으로** 사이트가 갱신됩니다.
전체 3단계, 약 5~10분이면 됩니다.

> 준비물: GitHub 계정(만드셨죠 👍), 웹브라우저

---

## 1단계 — GitHub에 코드 올리기

GitHub는 코드를 보관하는 창고예요. 여기 올려두면 Vercel이 읽어갑니다.

1. 브라우저에서 **https://github.com/new** 접속 (로그인 상태에서)
2. **Repository name** 칸에 원하는 이름 입력 → 예: `blog`
3. 나머지는 그대로 두고, 맨 아래 초록색 **Create repository** 클릭
4. 다음 화면에서 **"uploading an existing file"** (파란 링크) 클릭
5. 내 컴퓨터에서 **블로그 폴더를 열고**, 안에 있는 파일·폴더를 **전부 선택해서**
   브라우저 업로드 영역으로 **끌어다 놓기(드래그 앤 드롭)**
   - 올릴 것: `src`, `public`, `astro.config.mjs`, `package.json`,
     `tsconfig.json`, `.gitignore`, `README.md` 등 폴더 안 전부
   - (무거운 `node_modules` 폴더는 원래 없으니 신경 안 쓰셔도 돼요)
6. 아래로 내려가 초록색 **Commit changes** 클릭

> 파일이 많아 보여도 용량은 작아서 금방 올라갑니다.

---

## 2단계 — Vercel에 연결하기 (자동 배포)

Vercel이 GitHub의 코드를 읽어서 진짜 웹사이트로 만들어 줍니다. 무료예요.

1. 브라우저에서 **https://vercel.com** 접속
2. **Sign Up** → **Continue with GitHub** 선택 (GitHub 계정으로 바로 가입)
3. 로그인/권한 허용을 마치면 대시보드가 나옵니다.
4. **Add New...** → **Project** 클릭
5. 방금 만든 `blog` 저장소 옆의 **Import** 클릭
6. 설정은 손댈 것 없습니다. Vercel이 Astro를 자동 인식해요. 그대로 **Deploy** 클릭
7. 1~2분 기다리면 축하 화면과 함께 **주소(예: `blog-xxxx.vercel.app`)** 가 나옵니다.

이 주소가 회산 님의 블로그 인터넷 주소예요. 이제 누구나 볼 수 있습니다. 🎉

---

## 3단계 — 마무리 (제가 도와드립니다)

배포 후 주소를 알려주시면, 제가 `astro.config.mjs`의 사이트 주소를
실제 주소로 바꿔드릴게요. (검색 노출·RSS에 필요해요.)

---

## 앞으로 글 올리고 고치는 흐름

이제부터는 이렇게 돌아갑니다:

1. 저(Claude)에게 "글 써줘 / 고쳐줘" → 제가 폴더의 파일을 수정
2. 그 변경을 GitHub에 반영 → Vercel이 **자동으로 다시 배포**
3. 잠시 뒤 사이트에 반영 완료

2번(변경을 GitHub에 올리기)을 매번 편하게 하려면, **GitHub Desktop**
프로그램을 설치하면 좋아요. 폴더를 연결해두면 버튼 한 번으로 올라갑니다.
원하시면 이것도 순서대로 안내해 드릴게요.
